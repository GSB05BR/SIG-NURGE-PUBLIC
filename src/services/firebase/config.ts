/**
 * STUB de configuração — o showcase público NÃO usa Firebase real.
 *
 * O app original inicializava o Firebase aqui. Nesta versão de demonstração,
 * todo o acesso a dados é servido por um banco em memória (`src/mock/db.ts`),
 * então não há `initializeApp`, credenciais nem conexão de rede. Estes exports
 * existem apenas para satisfazer eventuais imports legados.
 */
export const app = null;
export const auth = null;
export const db = null;
export const storage = null;
