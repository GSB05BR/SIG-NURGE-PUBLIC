# Showcase público — NÃO contém segredos.
#
# O app original lia credenciais do Firebase daqui; nesta versão de demonstração
# não há Firebase (ver src/services/firebase/config.ts). O único valor abaixo é
# um e-mail FICTÍCIO usado apenas para que a persona "Distribuidor Demo" entre
# como super-admin no fluxo de autenticação simulado.
VITE_SUPER_ADMINS=distribuidor.demo@exemplo.com
